Comandos:

Rodar o pnpm run dev normalmente
abrir outro terminal cmd

nesse outro terminal, executar -> npm install -g cloudflared

executar -> cloudflared tunnel --url http://localhost:3000 (atenção, a porta vai mudar de acordo com a porta selecionada ao executar o comando "pnpm run dev")

copiar o link dentro da caixinha "your quick tunnel has been created! Copiar esse link e jogar numa nova aba do navegador.


